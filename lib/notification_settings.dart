class NotificationSettings {
  // Default offset time in minutes before the event
  static int defaultNotificationOffset = 10;
  // Default postpone time in minutes for shifting event start time
  static int defaultPostponeTime = 10;
}
